fn main() {
    println!("Hello, world!");
}

// cargo new -> starts new cargo project
// cargo build -> builds the file (compiles), and by default the executable file is stored in debug folder
// cargo run -> compiles and runs the file at the same time
// cargo check
// cargo build --release --> optimizes code for release